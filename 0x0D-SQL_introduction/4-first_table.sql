-- Creates a table called first table
-- the database name will be passed as an argument
--first table fields
--		id: INT
--		name: VARCHAR(256)
-- If the first_table exits should not fail
CREATE TABLE IF NOT EXISTS first_table (id INT, name VARCHAR(256));

