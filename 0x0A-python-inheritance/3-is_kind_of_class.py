#!/usr/bin/python3
""" checks if an object is an instance"""


def is_kind_of_class(obj, a_class):
    """ returns true if the object is an instance of a given class"""

    return (isinstance(obj, a_class))
